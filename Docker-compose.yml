version: '3.6'

services:
  dps-scheduler:
    image: ghcr.io/phamleduy04/texas-dps-scheduler:latest
    container_name: texas-dps-scheduler
    volumes:
      - ./config.yml:/home/container/config.yml